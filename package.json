{
  "name": "fotmob-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Fotmob API with Cloudflare bypass",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && npx puppeteer browsers install chrome",
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "puppeteer": "^22.6.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
